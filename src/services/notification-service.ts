import * as Notifications from 'expo-notifications';import type { TaskInput } from '@/types/task';import { fromDateKey } from './date-service';
export async function scheduleReminder(task:TaskInput){if(task.notificationOffset==null||!task.time)return null;const permission=await Notifications.requestPermissionsAsync();if(!permission.granted)return null;const trigger=fromDateKey(task.date);const[h,m]=task.time.split(':').map(Number);trigger.setHours(h,m-task.notificationOffset,0,0);if(trigger<=new Date())return null;return Notifications.scheduleNotificationAsync({content:{title:'Тапсырма уақыты келді',body:task.title},trigger:{type:Notifications.SchedulableTriggerInputTypes.DATE,date:trigger}})}
export async function cancelReminder(id:string|null){if(id)await Notifications.cancelScheduledNotificationAsync(id)}
