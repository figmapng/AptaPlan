import { useEffect,useState } from 'react';import { Alert,ActivityIndicator } from 'react-native';import { router,useLocalSearchParams } from 'expo-router';import { TaskForm } from '@/components/task-form';import { usePlanner } from '@/store/planner-store';import type { Task } from '@/types/task';
export default function EditTask(){const{id}=useLocalSearchParams<{id:string}>();const{get,update,remove}=usePlanner();const[task,setTask]=useState<Task|null>();useEffect(()=>{void get(id).then(setTask)},[id,get]);if(task===undefined)return <ActivityIndicator style={{flex:1}}/>;if(task===null)return null;const confirm=()=>Alert.alert('Тапсырманы өшіру','Бұл тапсырманы өшіргіңіз келе ме?',[{text:'Болдырмау',style:'cancel'},{text:'Өшіру',style:'destructive',onPress:async()=>{await remove(id);router.back()}}]);return <TaskForm initial={task} onSave={async input=>{await update(id,input);router.back()}} onDelete={confirm}/>}
